apiVersion: skaffold/v4beta4
kind: Config
build:
  local:
    push: false
  artifacts:
    - image: danielfr9/auth
      context: auth
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
      docker:
        dockerfile: Dockerfile
manifests:
  rawYaml:
    - ./infra/k8s/*
deploy:
  kubectl: {}
